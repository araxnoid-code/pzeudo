## 0.0.2-dev.1_plan.md
- ~~Weight Initialization~~
  - ~~He Initialization~~
  - ~~Xavier Initialization~~

- ~~No Grad Tensor~~
  - ~~eval and training Mode~~

- Optimizer
  - Sgd + Momentum
  - AdaGrad
  - RMSprop
  - Adam

- Saving
  - save
  - load

##### 0.0.2-dev.1-dev
_1 Aug 2026_
- create plan list for version 0.0.2-dev.1

##### 0.0.2-dev.1-no_grad
_1 Aug 2026_
- added new methods to StorageGrad
  - StorageGrad::remove_grad
  - StorageGrad::no_grad

- To enable the no_grad feature (mainly making a tensor have no_grad status or not have a gradient which is automatically not involved in backwards), StorageGrad is given an additional handler to perform validation using: 
  - time, each push, remove and no_grad operation will add time to a certain index (every index has a time). Useful for preventing a tensor from accessing a gradient that doesn't belong to it because mid-way through a tensor is set no_grad which will delete the gradient at its index, but after that the index is reused for the gradient of the new tensor. 
  - status, each index will have a status, status is divided into 3, namely: 
    - 0: Remove 
    - 1: no grad 
    - 2: value 
  By dividing this state, the tensor that accesses the gradient index that is deleted because no_grad will have a different error and will be handled further, differently from the gradient that is deleted because of remove.

- Due to updated changes that have occurred above. then the StorageType enum in the StorageType::Arr field has an additional value, namely `StorageType::Arr(usize, Option<usize>)` 
  - Likewise what happens to ViewStorageType::storage which becomes `ViewStorageType::Storage(usize, Option<usize>)`

_2 Aug 2026_
- Added a generic trait to the Tensor `Tensor<F, T, G>`, which serves as a marker for whether the tensor has a Grad or not and also handles the creation of a tensor without a gradient.
  - Type G must implement GradStatTrait. Types that implement it include:
    - `Grad`, use if you want a tensor with a grad
    - `NoGrad`, use if you want a tensor without a grad
  - The main change to initializing a tensor using a Module (except permanent ones) is that it is now required to initialize it to the Grad or NoGrad type.

_2 Aug 2026_
- Added the GradStatTrait::zeros_grad method to determine whether or not to create a gradient. Grad and NoGrad implement this trait and method.

- Added the `Tensor<F, Contiguous, Grad>::no_grad` method to disable gradients on tensors that have gradients.
- Currently, this only works for gradients in GradStorage. If the gradient is permanent or in metadata (view), it will return Err.

- Added the `Tensor<F, Contiguous, NoGrad>::with_grad` method to add a gradient to a tensor that initially has no_grad status.

_2 Aug 2026_
- The tools folder in pzeudo_tensor is now a single file called tools.rs

- Added GradStorage methods:
  - GradStorage::check_time_not_match
  - GradStorage::no_grad

- Added the tools function check_no_grad_or_time_not_match (in tools.rs)

- add a new parameter to Tensor::add, namely grad, which will accept a data type that implements GradStatTrait

- Modified the add_backward function to use check_no_grad_or_time_not_match to handle no_grad.
  - is of a testing nature, if successful it will be used in other backward functions

- The mechanism for deleting values in GradStorage (remove) that are unrelated to no_grad has been made a private method, as there is no handler for removing grad.

_3 Aug 2026_
- fixed the check_no_grad_or_time_not_match function, now if the input is StorageType::Permanent or StorageType::View, it will immediately return Ok(false).

- implemented GradStatTrait on add, sub, mul and div. then add additional parameters to determine Grad or NoGrad status.

- changed the name of GradStatTrait to RequiresGradTrait.

- mengimplementasikan RequiresGradTrait pada:
  - Tensor::add
  - Tensor::sub
  - Tensor::sub

- implemented the check_no_grad_or_time_not_match function in the method: 
  - sub_backward
  - mul_backward 
  - div_backward

_3 Aug 2026_
- in method: 
  - Tensors::add 
  - Tensor::sub 
  - Tensor::sub 
  There was an error in the previous commit, namely that every time it was used it would produce a Tensor of the gradient type Grad even though it was set to NoGrad, now it has been fixed.

- added RequiresGradTrait::into_zeros_grad method 
  - The RequiresGradTrait::into_zeros_grad method is used in methods that take RequiresGradTrait as a parameter. 
    - changes occurred in the method that uses RequiresGradTrait::zeros_grad but has parameters to determine the Grad or NoGrad data type, now the method uses RequiresGradTrait::into_zeros_grad instead of RequiresGradTrait::zeros_grad.

- implemented RequiresGradTrait on: 
  - matmul 
    - Tensor::matmul_2d(f32 and f64) 
    - Tensor::matmul_nd(f32 and f64) 
  - unary 
    - Tensor::exp 
    - Tensor::powi 
    - Tensor::powf 
    - Tensor::sqrt 
    - Tensors::log 
    - Tensors::log2 
    - Tensors::log20 
    - Tensor::ln

- implemented the check_no_grad_or_time_not_match function in the method: 
  - matmul 
    - matmul_2d_f32_backward 
    - matmul_2d_f64_backward 
    - matmul_nd_f32_backward 
    - matmul_nd_f64_backward 
  - unary 
    - exp_backward 
    - log_backward 
    - ln_backward 
    - powi_backward 
    - powf_backward 
    - sqrt_backward

_4 Aug 2026_
- implemented RequiresGradTrait on: 
  - relu
  - sigmoid
  - softplus
  - tanh
  
- implemented the check_no_grad_or_time_not_match function in the method:   
  - relu_backward
  - sigmoid_backward
  - softplus_backward
  - tanh_backward

_4 Aug 2026_
- implemented RequiresGradTrait on: 
  - mse
  - mae
  - cross_entropy_loss
  
- implemented the check_no_grad_or_time_not_match function in the method:   
  - mse_backward
  - mae_backward
  - cross_entropy_loss_backward

_4 Aug 2026_
- Rename RequiresGradTrait to ReqGradTrait.

- Provide handling for StorageType::View in the check_no_grad_or_time_not_match function.
  - Now, tensors with view properties can be affected by NoGrad tensors.
  - Because view tensors don't have gradients (view tensors point to the parent (contiguous) gradient along with its metadata), they don't have the no_grad and with_grad features. If you want to perform a no_grad, perform it on the parent contiguous tensor.
  - The Tensor View gradient state will follow the gradient state of the contiguous tensor.

- Now, the Tensor::no_grad (on type Grad) and Tensor::with_grad (on type no_grad) methods will consume self and return a new Tensor (with the same data as the consumed tensor, the only differences being the grad_idx property value and the ReqGradTrait type).

- When a tensor is set to no_grad, the gradient of that tensor is removed. When a tensor is set to with_grad, the tensor is allocated a new gradient.
  - Because of this, if there is a record from an operation while the tensor is NoGrad, that record will not store the gradient index. When the tensor is set to with_grad, the previously stored record will remain unchanged and will still not store the gradient index.

- no_grad on the permanent tensor has not yet been created.

_5 Aug 2026_
- Fixed a bug where the array was not being replaced at the empty index location in GradStorage::grad_push.

- Modified the PermanentTensor structure to allow for cases where no gradient is present. 
  - This structural change necessitated some code adjustments, though the workflow remains the same.

- Simplified error handling in pzeudo_tensor/error.rs; storage-related errors are now consolidated into StorageErr (with a few exceptions). 
  - Some fields in PzeudoErr were renamed, but their underlying meaning remains unchanged.

- Renamed PermanentTensor to ParamTensor.
  - Corresponding code updates were made to change PermanentStorage references to ParamStorage. 
  - ParamsStorage was created and is now a distinct component within ArrayStorage. 
  - Added the following methods to ParamsStorage:
    - ParamsStorage::new
    - ParamsStorage::push
    - ParamsStorage::with_grad
    - ParamsStorage::no_grad

- Added a no_grad feature for tensors of the Param type.

_5 Aug 2026_
- Fixed a bug where the array was not being replaced at the empty index location in ArrStorage::grad_arr.

_5 Aug 2026_
- combining PzeudoErr fields which tend to have the same core and removing unused fields, now PzeudoErr is more concise with divisions including: 
  - Tensor Tools 
  - Arrays 
  - GradStats 
  - Ops 
  - Storage 
  - Backwards 
  - Losses

- there is a change in tensor initialization 
  - Modules can now not initialize Tensors, Tensor initialization now directly uses the Tensor itself by including &Module as its argument. 
  - changes to all tensor initialization methods, the core changes are adding removing record and storage parameters and adding module parameters and also requires_grad.

_5 Aug 2026_
- Created the F32F64MatmulTensor trait to unify f32 and f64 functionality; this eliminates the need to manually write separate code for f32 and f64 in methods built upon this trait.
  - Currently, this applies to the F32F64MatmulTensor::matmul_2d method.
  - Implemented this in Linear, consolidating Linear::forward into a single implementation.

- Fixed a bug where epoch records were not cleared upon epoch completion; the Module::epoch method now clears record data after each epoch finishes.

- Added the Module::reset method, which clears storage (excluding parameters) and records; this method is called at the end of every epoch.

- Created a test model named linear_model_test_mse_f32_train_eval in pzeudo_tensor/_test/model/linear_layer.rs.
  - The test involves executing the training mode followed by the testing mode within the same epoch. 
    - Since the system uses storage similar to an arena allocator, the Module::reset method must be called before entering the evaluation/testing phase to clear tensors generated during the training phase.

_5 Aug 2026_
- Renamed the method directory to layer within pzeudo_tensor.

- Added rng(smallrng) to Module:
  - Module initialization (Module::new) now requires a seed for the RNG.

- Added a WeightInit enum with the following variants:
  - WeightInit::Xavier
  - WeightInit::He

- Implemented WeightInit for the linear layer.

##### 0.0.2-dev.1-optim
_5 Aug 2026_
- added a new optimizer: SgdMomentum

- added a method to Sgd
  - Sgd::set_lr
