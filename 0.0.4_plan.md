# 0.0.4_plan.md
## 0.0.4-dev.1
- Development of tensor initialization
	- epoch
		- Initialization outside the epoch
		- Initialization within the epoch
	- ~~optimization of tensor initialization~~
	- scalar initialization of a tensor

- A composite loss function combining Cross-Entropy Loss and softmax.

- Optimizing indexing for ArrayRef and ArrayRefMut

- adding customization to Weight Init

### Changelog
_06 Sep 2026_
- Create the 0.0.4 plan.

- update version.md, README.md and project version

_06 Sep 2026_
- perubahan inisialisasi pada Array
  - merubah method Array::from_vector_with_shape yang sebelumnya menerima slice kini akan menerima vector.
  - menambahkan method Array::from_slice_with_shape yang memungkinkan membuat array menggunakan slice.
  - Perubahan inisialisasi array ini menyebabkan perubahan pada inisialisasi tensor dan method yang terkait.
    - Inisialisasi Tensor
      - perubahan Tensor::from_vector_with_shape kini akan menerima vector
      - perubahan Tensor::param_from_vector_with_shape kini akan menerima vector
      - menambahkan Tensor::from_slice_with_shape
      - menambahkan Tensor::param_from_slice_with_shape
    - Method yang terkait dengan perubahan inisialisasi pada Array telah diperbarui
    - code test yang terkait dengan perubahan inisialisasi pada Array telah diperbarui
